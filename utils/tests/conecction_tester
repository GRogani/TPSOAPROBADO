#include "../utils.h"

int main()
{

    int connection_fd = create_connection("30002", "127.0.0.1");


    send_handshake(connection_fd, "Ricardo Fort");
    close(connection_fd);

    return 0;
    
}