#ifndef TIME_UTILS_H
#define TIME_UTILS_H

#include <stdint.h>

/**
 * @brief Obtiene el timestamp actual en milisegundos
 * @return uint64_t Timestamp en milisegundos
 */
uint64_t now(void);

#endif /* TIME_UTILS_H */
